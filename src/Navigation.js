const NavBar = () => {

    return (
        <nav id="nav_bar">
            <a href="#">Home</a>
            <a href="#">Products</a>
            <a href="#">About</a>
            <a href="#">Contact</a>
        </nav>
    );

}

export default NavBar;